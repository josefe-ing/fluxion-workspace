# ==========================================
# Fluxion AI - DuckDB Architecture
# ==========================================

# ============ DATABASES ============
# DuckDB databases (PRODUCTION DATA - DO NOT COMMIT)
data/
*.db
*.duckdb
*.db-journal
*.db-wal
*.db.tmp/
data/*.db.tmp/
data/*_temp.db

# NOTA IMPORTANTE: Las bases de datos NO se suben a GitHub
# Son demasiado grandes (16GB+) y contienen datos de producción
# En producción, las DBs se crean vacías y se llenan con ETL

# ============ ETL ============
etl/logs/
etl/temp/
etl/reports/*.json

# ============ NODE.JS (Frontend) ============
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn
dist/
build/

# ============ PYTHON (Backend) ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/

# ============ LOGS ============
*.log
logs/
*.pid
.fluxion/

# ============ BACKUPS ============
backups/
*.backup
*.tar.gz
*.zip

# ============ IDE ============
.vscode/
.idea/
*.swp
*.swo
*~

# ============ OS ============
.DS_Store
Thumbs.db
*.tmp

# ============ ENVIRONMENT ============
# IMPORTANT: ALL .env files contain secrets and should NOT be committed!
.env
.env.local
.env.development
.env.production
.env.test
.env.staging
.env.*.local
backend/.env
backend/.env.development
backend/.env.production
backend/.env.test
backend/.env.staging

# Exception: .env.example files are templates without secrets (safe to commit)
!.env.example
!backend/.env.example

# Other credentials
credentials.json
**/credentials/
**/*.pem
**/*.key
!**/public.key

# ============ TEMPORARY ============
temp/
tmp/
*.tmp/

# Development logs
/tmp/fluxion_*.log
/tmp/fluxion_*.pid

# ============ CDK / INFRASTRUCTURE ============
infrastructure/cdk.out/
cdk.out/

# ETL Reports (temporary files)
**/reporte_ventas_*.txt
**/reporte_ventas_*.json
etl/test_copy_optimization.py
venv_old_python39/
