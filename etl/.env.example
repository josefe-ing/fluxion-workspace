# ========================================
# Fluxion AI - ETL Environment Variables
# ========================================

# ========================================
# EXTRACTOR DE VENTAS - Seleccionar método
# ========================================

# Método de extracción de ventas:
#   - "sql_server": Conectar directamente a SQL Server de cada tienda (método actual)
#   - "api": Consumir API REST del nuevo proveedor POS (método nuevo)
VENTAS_EXTRACTOR_TYPE=sql_server

# ========================================
# CONFIGURACIÓN API (solo si VENTAS_EXTRACTOR_TYPE=api)
# ========================================

# URL base del API del nuevo proveedor POS
POS_API_BASE_URL=https://pos-api.ejemplo.com

# API Key para autenticación
POS_API_KEY=your-api-key-here

# Timeout para requests HTTP (en segundos)
POS_API_TIMEOUT=600

# Número máximo de reintentos en caso de error
POS_API_MAX_RETRIES=3

# Delay entre reintentos (en segundos)
POS_API_RETRY_DELAY=5

# ========================================
# CONFIGURACIÓN SQL SERVER (solo si VENTAS_EXTRACTOR_TYPE=sql_server)
# ========================================

# SQL Server Credentials (La Granja Mercado)
SQL_USER=beliveryApp
SQL_PASS=your_password_here

# WireGuard VPN Configuration
# Local IP when connected via VPN: 10.32.0.24/32
# Network access: 192.168.x.x (La Granja internal network)
# DNS: 192.168.20.55

# ========================================
# CONFIGURACIÓN GENERAL
# ========================================

# DuckDB Database Path
DATABASE_PATH=../data/fluxion_production.db

# Sentry Monitoring - Cron Monitors para ETL
# Obtén tu DSN en: https://sentry.io/settings/projects/etl-process/keys/
SENTRY_DSN=https://your-public-key@oXXXXXX.ingest.us.sentry.io/XXXXXXX

# Sentry Environment (development, staging, production)
SENTRY_ENVIRONMENT=development

# Sentry Traces Sample Rate (0.0 a 1.0) - % de transacciones a trackear
SENTRY_TRACES_SAMPLE_RATE=0.1

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=INFO
